type RootQuery {
  authors(size: Int!, offset: Int!): [Author]
  author(name: String!): Author
  poem(author: String!, title: String): Poem
  painting(title: String): Painting
  paintings(painter: String): [Painting!]!
}
